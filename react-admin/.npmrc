# 不自动安装缺失的 peer 依赖（含 optional peers）：
# @vitejs/plugin-react 的 optional peers（@rolldown/plugin-babel、babel-plugin-react-compiler）
# 在 oxc 原生 Compiler 路线下不使用，避免被自动装回 node_modules 产生残留与 peer 警告
auto-install-peers=false
